<h1><%= translate "hello" %> </h1>
<%= link_to "Translate", request.query_parameters[:locale] == "hi" ? url_for(locale: nil) : url_for(locale: "hi") %>

<%= link_to "Login", new_session_path if !authenticated? %>

<h1>Products</h1>

<%= link_to translate("New product"), new_product_path if authenticated? %>

<div id="products">
  <% @products.each do |product| %>
<div>
  <%= link_to product.name, product_path(product.id) %>
</div>
<%end%>
</div>
